<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bg50xx.assignment">

    <uses-permission android:name="android.permission.INTERNET"/>
    <permission android:name="com.example.bg50xx.assignment.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.example.bg50xx.assignment.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="false"
        android:largeHeap="true">
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".Ticket"
            android:label="Assignment">
        </activity>
        <activity
            android:name=".MasterView"
            android:label="Assignment">
        </activity>
        <provider
            android:authorities="com.example.bg50xx.assignment"
            android:name=".ArtworkProvider"
            android:exported="true"
            android:readPermission="com.example.bg50xx.assignment.READ_DATABASE"
            android:writePermission="com.example.bg50xx.assignment.WRITE_DATABASE"
            />
    </application>

</manifest>